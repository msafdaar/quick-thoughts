Now we need to add the 'commit feature'. User might select some part of text on main screen, and press a button to 'Commit Selected'. There should also be a button to 'Commit All'

what commit does?
it takes the whole draft /or user selected section of draft, cuts it from the draft section and saves it inside the main file just before the draft section starts. 

buttons that need to be there